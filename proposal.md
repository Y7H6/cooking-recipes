### Epicure × Jev × Qwen3.8 27B 味覚AI PoC 企画書  
（Next.js + FastAPI + PostgreSQL 構成）

---

## 1. 背景・目的

**背景:**

- 生成AIによるレシピ提案は増えているが、「味の定量評価」や「好みに合わせた最適化」が弱い。
- Epicure により食材を 300 次元ベクトルで扱えるため、味・文化・食感を数値化できる土台がある。
- Jev（TypeSafe AI）は「型付き＋確率付き」の評価器として、味スコアを構造化データで返せる。

**目的:**

- Epicure × Jev × Qwen3.8 27B Local を組み合わせて  
  **「レシピ→味スコア」「材料→レシピ生成＋味評価」を行う味覚AIエンジンの PoC** を構築する。
- 技術スタックを **Next.js + FastAPI + PostgreSQL** に統一し、将来の拡張を見据えた構成にする。

---

## 2. コンセプト

**コンセプト：  
「味覚を数値で扱うレシピ評価・生成エンジン」を Web アプリとして実装する。**

- **Epicure**：1,790 食材 × 300 次元ベクトルの味覚空間モデル  
- **Jev API**：  
  - 型定義に沿って「甘さ・辛さ・旨味・総合おいしさ」を数値＋確率で返す評価器  
- **Qwen3.8 27B Local**：  
  - 材料・制約条件からレシピ案を生成するローカル LLM  
- **Next.js**：  
  - レシピ入力・味スコア可視化・レシピ生成 UI  
- **FastAPI + PostgreSQL**：  
  - 味覚計算ロジック・LLM連携・データ永続化

---

## 3. システム構成

### 3-1. 全体アーキテクチャ

- **フロントエンド：Next.js**
  - ページ例：
    - **/recipes/evaluate**：レシピ入力＋味スコア表示  
    - **/recipes/generate**：材料入力＋レシピ生成＋評価  
  - グラフ表示（レーダーチャートなど）で味スコアを可視化。

- **バックエンド：FastAPI**
  - Epicure CSV 読み込み・食材ベクトル管理
  - 味特徴量計算（食材→ベクトル→特徴量）
  - Jev API クライアント（味スコア評価）
  - Qwen3.8 27B Local クライアント（レシピ生成）
  - PostgreSQL へのレシピ・評価結果保存

- **DB：PostgreSQL**
  - **テーブル例：**
    - `ingredients`：Epicure 食材ID・名称・カテゴリ  
    - `recipes`：レシピ本文・生成元（手入力/LLM）  
    - `taste_scores`：甘さ・辛さ・旨味・総合スコア＋信頼度  
    - `recipe_ingredients`：レシピと食材の紐付け＋分量  

### 3-2. FastAPI モジュール構成

- **epicure_loader.py**
  - `epicure_core.csv` 読み込み  
  - `ingredient_id -> vector[300]` のマッピング構築  

- **taste_feature_extractor.py**
  - 食材リスト＋分量 → ベクトル合成  
  - ベクトル → 味特徴量（甘さ軸・辛さ軸など）抽出（線形変換 or 事前定義）

- **jev_client.py**
  - Jev API に対して：
    - 入力：レシピテキスト＋味特徴量  
    - 出力：  
      ```json
      {
        "sweet": 0-100,
        "spicy": 0-100,
        "salty": 0-100,
        "bitter": 0-100,
        "umami": 0-100,
        "overall_taste": 0-100,
        "confidence": 0-1
      }
      ```

- **qwen_client.py**
  - ローカル Qwen3.8 27B へのプロンプト送信  
  - 材料＋制約条件 → レシピ案生成

- **routes_recipes.py**
  - `POST /api/recipes/evaluate`  
    - 入力：材料＋作り方  
    - 出力：味スコア＋特徴量  
  - `POST /api/recipes/generate`  
    - 入力：材料＋制約条件  
    - 出力：生成レシピ＋味スコア＋改善提案

---

## 4. PoC 機能

### 4-1. 機能①：レシピ→味スコア評価

**入力：**

- 材料（食材名＋分量）  
- 作り方テキスト  

**処理：**

1. 食材名を Epicure 食材IDにマッピング  
2. ベクトル合成 → 味特徴量抽出  
3. Jev API に評価依頼 → 味スコア取得  
4. PostgreSQL にレシピ＋スコア保存  
5. Next.js でレーダーチャート表示

**出力：**

- 甘さ・辛さ・旨味・塩味・苦味・総合おいしさ  
- スコアの信頼度（Jev の確率）

---

### 4-2. 機能②：材料→レシピ生成＋味評価

**入力：**

- 材料リスト  
- 制約条件（例：和食風、辛さ控えめ、調理時間30分以内）

**処理：**

1. Qwen3.8 27B Local にプロンプト送信 → レシピ案生成  
2. 機能①と同じフローで味スコア算出  
3. スコアが低い場合、  
   - 「辛さが高い → 唐辛子減らす」  
   - 「甘さ不足 → みりん追加」  
   などの改善指示を Qwen に再プロンプト  
4. 改善後レシピを再評価し、スコア比較を表示

**出力：**

- 初回レシピ＋味スコア  
- 改善後レシピ＋味スコア  
- 改善度（スコア差分）

---

### 4-3. 機能③：履歴・比較ビュー

- 過去レシピの一覧・詳細  
- 同じ材料セットで生成した複数レシピの味スコア比較  
- 「この材料セットで最もおいしいレシピ」のランキング表示

---

## 5. 期待効果

- **味覚の定量化**：  
  感覚的な「甘い・辛い」を数値で扱い、議論・改善がしやすくなる。
- **生成レシピの品質向上**：  
  Qwen の生成結果を Jev＋Epicure で評価・フィードバックすることで、外れレシピを減らせる。
- **将来展開**：  
  - ユーザーごとの味覚プロファイル（甘さ80/辛さ20など）に合わせたレシピ自動生成  
  - 食品開発・レシピサービスへの応用

---

## 6. PoC ロードマップ

### フェーズ1：基盤構築（2〜3週間）

- Epicure CSV 解析・食材マスタ設計  
- FastAPI + PostgreSQL プロジェクト雛形  
- Next.js プロジェクトセットアップ

### フェーズ2：味スコアAPI実装（3〜4週間）

- 食材→ベクトル→味特徴量ロジック  
- Jev API クライアント実装  
- `/api/recipes/evaluate` 実装  
- Next.js で評価UI・グラフ表示

### フェーズ3：レシピ生成＋最適化（3〜4週間）

- Qwen3.8 27B Local クライアント実装  
- `/api/recipes/generate` 実装  
- 生成→評価→改善ループ  
- UI に生成・改善ボタン追加

### フェーズ4：検証・デモ（2週間）

- 代表的な材料セットでデモシナリオ作成  
- 人間の主観との比較で味スコアの妥当性確認  
- デモ用画面・説明資料整備

---
